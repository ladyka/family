spring:
  datasource:
    database: familytest
    url: jdbc:h2:mem:${spring.datasource.database};MODE=PostgreSQL
    driverClassName: org.h2.Driver
    username: user
    password: password
  liquibase:
    change-log: classpath:/db/db.changelog-test.yaml

enable-tables-check: true
