spring:
  datasource:
    database: familytest
    url: jdbc:h2:file:/tmp/familytest;MODE=PostgreSQL;AUTO_SERVER=TRUE
    driverClassName: org.h2.Driver
    username: user
    password: password
  liquibase:
    change-log: classpath:/db/db.changelog-test.yaml
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect

enable-tables-check: true
